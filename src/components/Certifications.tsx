import React from 'react';

const Certifications = () => {
  // This component is not needed as we combined it with the Education section
  // Keeping this as an empty component to avoid breaking references
  return (
    <></>
  );
};

export default Certifications;